name: ZK Deploy
on: [push, schedule]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: docker build -t zk-revenue .
    - run: echo "🚀 ZK Revenue deployed globally"
